<?xml version="1.0" encoding="UTF-8"?>
<suite name="LambdaTest Hackathon Suite">
    <!-- Parameters for browser, version, and platform -->
    <parameter name="browser" value="chrome"/>
    <parameter name="version" value="latest"/>
    <parameter name="platform" value="Windows 10"/>

    <!-- Test suite configuration -->
    <test name="LambdaTest Test">
        <parameter name="browser" value="chrome"/>
        <parameter name="version" value="latest"/>
        <parameter name="platform" value="Windows 10"/>

        <!-- Test classes -->
        <classes>
            <!-- Replace 'your.package.TestClass' with actual test class names -->
            <class name="tests.LambdaTestTest"/>
            <!-- Add more test classes as required -->
        </classes>
    </test>
</suite>
